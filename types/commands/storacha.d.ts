/**
 * Storacha Commands
 * Manage Storacha (IPFS network) authentication and configuration
 */
import { Command } from 'commander';
export declare function registerStorachaCommands(program: Command): void;
