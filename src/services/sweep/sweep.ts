import { FILE } from '../api/file';
import { makePOSTRequest } from '../api/api';

export const performSweep = async (): Promise<void> => {
  console.log('Sweep operation is a work in progress.');
};
